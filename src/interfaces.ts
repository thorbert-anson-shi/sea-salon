interface Person {
    name: string;
    phoneNo: string;
}

interface Service {
    title: string;
    src: string;
    content: string;
    // booking_url: string;
}

interface Review {
    name: string;
    stars: number;
    review: string;
}